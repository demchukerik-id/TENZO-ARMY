# Portraits

Drop generated images of Tenzo here. Suggested naming so the pair stays
recognizable across image-model runs:

```
tenzo-closeup-v1.png       (or .jpg, .webp — whatever the model outputs)
tenzo-closeup-v2.png
tenzo-fullbody-v1.png
tenzo-fullbody-v2.png
tenzo-console-v1.png       (the boss-in-the-seat / inner-circle-of-five shot)
tenzo-console-v2.png
```

If a render is the canonical/chosen one, rename it without the `-vN`
suffix:

```
tenzo-closeup.png
tenzo-fullbody.png
tenzo-console.png
```

The prompts that produced these images live in `../identity.md` under
**Face — image-model prompts**.
